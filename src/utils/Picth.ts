export default function (x1:number,y1:number){
    return x1/y1
}


/**
  const a1  = Math.abs(y1 - y2)
    const a2  = Math.abs(x1 - x2)
    const res = a1/a2
    // value of angle formula
    const angle  = Math.atan(res)*(180/Math.PI)
    return res 
 */